1. Loads geographic data from a KML file using geotable
2. Reads a shapefile using GeoPandas and extracts its bounding box
3. Uses NASA's CMR API to search for GEDI satellite data products within that bounding box
4. Prints the number of granules found and lists them
